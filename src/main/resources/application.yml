spring:
  datasource:
    url: "jdbc:mysql://localhost/board"
    username: "root"
    password: "Yuni14767!?"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    cache: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request_size: 100MB

logging.level:
  org.hibernate.SQL: debug
  org.springframework.security: debug


file.dir: C:/Users/bomin/Desktop/board-master/file/